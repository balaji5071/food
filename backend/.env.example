# Server
PORT=3000

# CORS – comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# Rate limiting – max requests per 15-minute window per IP
RATE_LIMIT_MAX=100

# Vendor authentication – set a strong secret to protect vendor-only endpoints
# When empty, vendor auth is disabled (development mode only!)
VENDOR_API_KEY=
